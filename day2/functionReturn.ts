function runMore(distance: number): number;

/*
function runMore(distance: number);
function runMore(distance: number): void;
*/

function runMore(distance: number) {
  return distance + 10;
}

console.log(runMore(20));
